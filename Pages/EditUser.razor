@page "/user/edit"
@using Snipster.Data;
@using Snipster.Services;
@using Snipster.Components
@using Microsoft.AspNetCore.Identity
@using Microsoft.AspNetCore.Components.Forms
@using static Snipster.Data.DBContext;


<EditForm Model="editUser" OnValidSubmit="HandleSave">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <div class="modal-content">

        <div class="modal-header">
            <h3>Edit User Details</h3>
        </div>

        <div class="form-group">
            <label>First Name:</label>
            <input type="text" class="form-input" @bind="editUser.FirstName" />
        </div>

        <div class="form-group">
            <label>Last  Name:</label>
            <input type="text" class="form-input" @bind="editUser.LastName" />
        </div>

        <div class="form-group">
            <label>Email:</label>
            <input type="email" class="form-input" disabled @bind="editUser.Email" />
        </div>


        <div class="modal-footer">
            <button class="save-btn" type="submit">Save</button>
            <button class="save-btn" type="button" @onclick="Cancel">Cancel</button>
            <button class="delete-account-btn" type="button" @onclick="ShowDeleteAccountModal">Delete Account</button>
        </div>

    </div>
</EditForm>

<Modal @ref="deleteAccountModal" Title="Delete Account">
    <div class="delete-warning">
        <p>This will permanently delete your account and all collections and snippets you created.</p>
        <p>This action cannot be undone.</p>
    </div>
    <div class="delete-actions">
        <button class="delete-confirm-btn" type="button" @onclick="HandleDeleteAccount">Delete Account</button>
        <button class="delete-cancel-btn" type="button" @onclick="CloseDeleteAccountModal">Keep Account</button>
    </div>
</Modal>

<Modal @ref="spinnerModal">

</Modal>


@code {


}
<style>

    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .modal-content, .form-content {
        background-color: #f9f9f9;
        padding: 25px;
        border-radius: 12px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        max-width: 500px;
        margin: 30px auto;
    }

    .modal-header h3 {
        margin: 0 0 15px 0;
        font-size: 1.5rem;
        color: #333;
    }

    .form-group {
        margin-bottom: 20px;
        display: flex;
        flex-direction: column;
    }

    .form-group label {
        font-weight: 600;
        margin-bottom: 6px;
    }

    label.inline {
        display: flex;
        align-items: center;
        gap: 8px;
    }

    input[type="text"],
    input[type="email"],
    input[type="password"],
    .form-input {
        padding: 8px 12px;
        border: 1px solid #ccc;
        border-radius: 8px;
        width: 100%;
        box-sizing: border-box;
        transition: border-color 0.2s ease-in-out;
    }

    input:focus,
    .form-input:focus {
        border-color: #007bff;
        outline: none;
    }

    .checkbox-element {
        width: 18px;
        height: 18px;
        cursor: pointer;
    }

    .save-btn,
    .btn-submit {
        background-color: #007bff;
        color: white;
        border: none;
        padding: 10px 18px;
        border-radius: 8px;
        cursor: pointer;
        font-weight: 600;
        transition: background-color 0.2s ease-in-out;
    }

    .save-btn:hover,
    .btn-submit:hover {
        background-color: #0056b3;
    }

    .delete-account-btn {
        background-color: #dc3545;
        color: white;
        border: none;
        padding: 10px 18px;
        border-radius: 8px;
        cursor: pointer;
        font-weight: 600;
        transition: background-color 0.2s ease-in-out;
    }

    .delete-account-btn:hover,
    .delete-confirm-btn:hover {
        background-color: #b02a37;
    }

    .delete-warning {
        color: #333;
        line-height: 1.45;
    }

    .delete-actions {
        display: flex;
        justify-content: flex-end;
        gap: 12px;
        margin-top: 20px;
    }

    .delete-confirm-btn {
        background-color: #dc3545;
        color: white;
        border: none;
        padding: 10px 18px;
        border-radius: 8px;
        cursor: pointer;
        font-weight: 600;
    }

    .delete-cancel-btn {
        background-color: #6c757d;
        color: white;
        border: none;
        padding: 10px 18px;
        border-radius: 8px;
        cursor: pointer;
        font-weight: 600;
    }


    .modal-footer {
        margin-top: 20px;
        display: flex;
        justify-content: flex-end;
        gap: 12px;
    }

    .validation-message {
        color: red;
        font-size: 0.9rem;
    }
</style>
